# Music_Player
User interface for playing music from a folder full of .mp3 files.

The UI.py file is the only file that is the interface (front-end), but it also contains the logic to run all the programming (back-end).
The code is based on tkinter for the UI and pygame to manage internal logic.
